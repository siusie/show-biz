<!DOCTYPE html>
<html>
<head>
    <!-- Do not modify the meta properties below -->
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="author" content="ShowBiz" />
    <meta name="application-name" content="2231-36e7b699-d027-4f78-a817-6b2c6d4d4a7a" />

    <title>@ViewBag.Title</title>

    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")

    @{
        var userAccount = new ShowBiz.Controllers.RequestUser(User as System.Security.Claims.ClaimsPrincipal);
    }
</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink("Actors & Shows", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Home", "Index", "Home")</li>
                    <li>@Html.ActionLink("Genres", "Index", "Genres")</li>
                    <li>@Html.ActionLink("Actors", "Index", "Actors")</li>
                    <li>@Html.ActionLink("Shows", "Index", "Shows")</li>
                    <li>@Html.ActionLink("Episodes", "Index", "Episodes")</li>

                    @*drop-down menu for initial loading of data from persistent store*@
                    @*<li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">Load Data&nbsp;<b class="caret"></b></a>
                        <ul class="dropdown-menu">
                            <li>@Html.ActionLink("Roles", "Roles", "LoadData")</li>
                            @if (userAccount.IsAdmin)
                            {
                                <li>@Html.ActionLink("Genres", "Genres", "LoadData")</li>
                                <li>@Html.ActionLink("Actors", "Actors", "LoadData")</li>
                                <li>@Html.ActionLink("Shows", "Shows", "LoadData")</li>
                                <li>@Html.ActionLink("Episodes", "Episodes", "LoadData")</li>

                                // remove data-->
                                <li>@Html.ActionLink("Remove actors", "RemoveActors", "LoadData")</li>
                                    <li>@Html.ActionLink("Remove episodes", "RemoveEpisodes", "LoadData")</li>
                                    <li>@Html.ActionLink("Remove shows", "RemoveShows", "LoadData")</li>
                            }
                        </ul>
                    </li>*@

                </ul>
                @Html.Partial("_LoginPartial")
            </div>
        </div>
    </div>
    <div class="container body-content">
        @RenderBody()
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
